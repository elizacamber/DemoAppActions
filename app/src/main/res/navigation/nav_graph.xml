<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/mainActivity">
    <activity
        android:id="@+id/mainActivity"
        android:name="com.elizacamber.todo.MainActivity"
        android:label="activity_main"
        tools:layout="@layout/activity_main" />
    <activity
        android:id="@+id/activeTasksActivity"
        android:name="com.elizacamber.todo.ActiveTasksActivity"
        android:label="activity_active_tasks"
        tools:layout="@layout/activity_active_tasks" />
    <activity
        android:id="@+id/completedTasksActivity"
        android:name="com.elizacamber.todo.CompletedTasksActivity"
        android:label="activity_completed_tasks"
        tools:layout="@layout/activity_completed_tasks" />
</navigation>